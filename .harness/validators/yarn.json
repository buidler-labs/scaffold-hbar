{
  "name": "tokenise-subscriptions-extend-yarn",
  "description": "Yarn-only Hardhat-aware target validation after the Subscription Guide extension. Named install allows fingerprint skip across attempts. Root yarn lint covers nextjs + hardhat.",
  "requiresNoSecrets": true,
  "forbiddenCommands": [
    "npm install",
    "npm run",
    "pnpm install",
    "pnpm run",
    "yarn foundry:*",
    "docker *"
  ],
  "commands": [
    {
      "name": "install",
      "command": "yarn install",
      "timeoutMs": 300000,
      "purpose": "Install Next.js and Hardhat workspaces with Yarn (pilot is yarn-only)."
    },
    {
      "name": "lint",
      "command": "yarn lint",
      "timeoutMs": 180000,
      "purpose": "Lint the extended frontend and Hardhat workspace."
    },
    {
      "name": "build",
      "command": "yarn next:build",
      "timeoutMs": 300000,
      "purpose": "Production build (includes TypeScript checks)."
    }
  ]
}
