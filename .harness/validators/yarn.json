{
  "name": "blank-template-extend-yarn",
  "description": "Yarn-only target validation after the Getting Started page extension. Named install allows fingerprint skip across attempts. Uses next:lint/next:build so Hardhat/Foundry tooling is not required for this frontend-only recipe.",
  "requiresNoSecrets": true,
  "forbiddenCommands": [
    "npm install",
    "npm run",
    "pnpm install",
    "pnpm run",
    "yarn hardhat:*",
    "yarn foundry:*",
    "docker *"
  ],
  "commands": [
    {
      "name": "install",
      "command": "yarn install",
      "timeoutMs": 300000,
      "purpose": "Install workspaces with Yarn (pilot is yarn-only)."
    },
    {
      "name": "lint",
      "command": "yarn next:lint",
      "timeoutMs": 180000,
      "purpose": "Lint the extended Next.js frontend."
    },
    {
      "name": "build",
      "command": "yarn next:build",
      "timeoutMs": 300000,
      "purpose": "Production build (includes TypeScript checks)."
    }
  ]
}
