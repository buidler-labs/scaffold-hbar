/**
 * With Reown AppKit + HederaAdapter there is no local hardhat chain to check.
 * Stub this hook so components that reference it still compile.
 */
export function useLocalChainConnectionError(): boolean {
  return false;
}
