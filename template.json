{
  "name": "payments-scheduler",
  "description": "HTS payments scheduler template with Foundry contracts.",
  "create-scaffold-hbar": {
    "capabilities": {
      "frontend": [
        "nextjs-app"
      ],
      "solidityFramework": [
        "foundry"
      ]
    },
    "defaults": {
      "frontend": "nextjs-app",
      "solidityFramework": "foundry"
    },
    "outro": {
      "steps": [
        "This template uses Hedera Schedule Service, which is not yet available in Foundry Hedera forking, so run it on Hedera testnet or mainnet (not on a local chain or forked Hedera network).",
        "+Suggested steps:",
        "1. Generate/import a deployer account: {run:foundry:account:generate}",
        "2. Compile contracts: {run:foundry:compile}",
        "3. Run tests: {run:foundry:test}",
        "4. Deploy to Hedera testnet: {run:foundry:deploy:testnet}",
        "5. Verify on Hashscan testnet: {run:foundry:verify:testnet}",
        "6. Start the frontend: {run:next:dev}",
        "After this, customize anything you want in contracts or frontend to fit your product."
      ]
    }
  }
}